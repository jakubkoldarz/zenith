# Zenith - Environment Configuration
# Copy this file to .env and customize the values

# Database Configuration
DB_USER=zenith_user
DB_PASSWORD=zenith_password
DB_NAME=zenith_db

# JWT Secret (IMPORTANT: Change this to a strong random string in production!)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Origins 
# Default: http://localhost:8080,http://localhost:3000,http://localhost:3001
CORS_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:3001
